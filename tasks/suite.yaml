# Kept task suite. Each task has an ideal tool path and the minimum steps.
- task_id: t1
  question: "What is 6 times 7?"
  reference_tools: [multiply]
  min_steps: 1

- task_id: t2
  question: "What is 5 plus 9?"
  reference_tools: [add]
  min_steps: 1

- task_id: t3
  question: "Add 3 and 4, then multiply the result by 2."
  reference_tools: [add, multiply]
  min_steps: 2

- task_id: t4
  question: "Multiply 8 by 3, then add 5 to that result."
  reference_tools: [multiply, add]
  min_steps: 2

- task_id: t5
  question: "Add 10 and 15, then add 4 more to the total."
  reference_tools: [add, add]
  min_steps: 2

- task_id: t6
  question: "Multiply 2 by 6, then multiply that result by 4."
  reference_tools: [multiply, multiply]
  min_steps: 2

- task_id: t7
  question: "Add 7 and 8, multiply the result by 3, then add 2."
  reference_tools: [add, multiply, add]
  min_steps: 3

- task_id: t8
  question: "Multiply 5 by 5, add 10 to it, then multiply the total by 2."
  reference_tools: [multiply, add, multiply]
  min_steps: 3

- task_id: t9
  question: "Add 4 and 6, multiply the result by 2, add 3, then multiply by 5."
  reference_tools: [add, multiply, add, multiply]
  min_steps: 4

- task_id: t10
  question: "Multiply 3 by 3, add 1, multiply by 4, then add 2."
  reference_tools: [multiply, add, multiply, add]
  min_steps: 4

- task_id: t11
  question: "Add 9 and 1, add 5, multiply by 2, then multiply by 3."
  reference_tools: [add, add, multiply, multiply]
  min_steps: 4

- task_id: t12
  question: "Multiply 2 by 2, multiply by 2, multiply by 2, then add 1."
  reference_tools: [multiply, multiply, multiply, add]
  min_steps: 4

- task_id: t13
  question: "Add 5 and 5, multiply by 3, add 10, multiply by 2, then add 4."
  reference_tools: [add, multiply, add, multiply, add]
  min_steps: 5

- task_id: t14
  question: "Multiply 4 by 2, add 6, multiply by 3, add 1, then multiply by 2."
  reference_tools: [multiply, add, multiply, add, multiply]
  min_steps: 5

- task_id: t15
  question: "Add 1 and 2, add 3, add 4, add 5, then multiply by 10."
  reference_tools: [add, add, add, add, multiply]
  min_steps: 5